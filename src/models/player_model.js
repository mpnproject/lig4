class Player {
  constructor(name, className) {
      this.name = name // Referência ao nome do jogador.
      this.className = className // Referência a classe dele no css, para denominar se será o jogador um ou dois e sua cor.
  }
}